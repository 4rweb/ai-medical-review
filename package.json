{
  "name": "ai-medical-review",
  "private": true,
  "version": "0.0.0",
  "description": "AI Medical Review — monorepo (frontend Vite/React + backend NestJS)",
  "scripts": {
    "dev": "concurrently -k -n api,web,mcp -c blue,green,magenta \"pnpm dev:apis\" \"pnpm dev:web\" \"pnpm dev:mcp\"",
    "dev:web": "pnpm --filter web dev",
    "dev:apis": "pnpm --filter apis dev",
    "dev:mcp": "pnpm --filter mcp dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "concurrently": "^10.0.3",
    "vitest": "^3.2.4"
  },
  "packageManager": "pnpm@11.8.0"
}
